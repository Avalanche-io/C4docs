Given(/^we have a BDD tool installed$/) do
  # if this runs at all then it's true
end

When(/^we implement a test$/) do
  raise "hell" if true != true
end

Then(/^a BDD tool will test it for us!$/) do
end

